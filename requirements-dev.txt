# Development-only tools (formatting). The pipeline and tests do not need these.
# Formatting convention is enforced via pyproject.toml ([tool.black], [tool.isort]).
-r requirements.txt
black==26.5.1
isort==8.0.1
